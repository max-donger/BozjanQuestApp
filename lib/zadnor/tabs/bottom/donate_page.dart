import 'package:flutter/material.dart';

class DonatePage extends StatelessWidget {
  DonatePage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('Donate Page - Tab 7', style: TextStyle(fontSize: 24))),
    );
  }
}
